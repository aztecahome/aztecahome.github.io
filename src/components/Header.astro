---
import AztecaLogo from "./AztecaLogo.astro";

const pathname = new URL(Astro.request.url).pathname;
const otherPathname =
  pathname.slice(-1) === "/" ? pathname.slice(0, -1) : pathname; // remove the last "/"
const paths = [
  { url: "/", name: "Home" },
  { url: "/pest-control", name: "Pest Control" },
  { url: "/pools", name: "Pools" },
  { url: "/landscaping", name: "Landscaping" },
  { url: "/contact", name: "Contact" },
];
---

<header
  class="bg-[var(--nav-background)] sm:flex sm:justify-between z-10 relative shadow-lg"
  
>
  <div class="flex w-auto justify-center">
    <a href="/">
      <AztecaLogo class="max-w-[225px]" 
      data-turbo-permanent id="headerLogo"
      />
    </a>
  </div>
  <div class="sm:flex sm:flex-col sm:justify-between">
    <div
      class="mx-2 my-1 flex h-full items-center justify-center sm:justify-end flex-wrap"
    >
      <svg
        class="h-[25px] w-[25px] fill-army-900 shrink-0"
        xmlns="http://www.w3.org/2000/svg"
        height="24px"
        viewBox="0 0 24 24"
        width="24px"
        fill="#000000"
      >
        <path
          d="M20.01 15.38c-1.23 0-2.42-.2-3.53-.56-.35-.12-.74-.03-1.01.24l-1.57 1.97c-2.83-1.35-5.48-3.9-6.89-6.83l1.95-1.66c.27-.28.35-.67.24-1.02-.37-1.11-.56-2.3-.56-3.53 0-.54-.45-.99-.99-.99H4.19C3.65 3 3 3.24 3 3.99 3 13.28 10.73 21 20.01 21c.71 0 .99-.63.99-1.18v-3.45c0-.54-.45-.99-.99-.99z"
        ></path>
      </svg>
      <a
        href="tel:602-926-2021"
        class="text-xl font-bold text-army-900 text-center">(602)-926-2021</a
      >
    </div>
    <nav class="font-semibold font-sans text-army-900">
      <ul class="flex flex-wrap justify-center pb-1 sm:pb-0 sm:grid sm:grid-cols-none sm:grid-flow-col-dense sm:auto-cols-max" role="menubar">
        {
          paths.map((path) => (
            <li class="contents" role="none">
              <a
                class={`rounded sm:rounded-none col-span-4 sm:col-auto text-center
                        sm:border-b-8 m-1 sm:m-0 sm:pb-2 px-3 py-3 transition-all ease-out
                      hover:bg-army-400
                        underline sm:no-underline whitespace-nowrap
                        sm:active:pb-0 sm:active:pt-4
                        ${pathname === path.url || otherPathname === path.url
                        ? "border-army-900 bg-army-900 text-army-50 no-underline sm:bg-transparent sm:text-army-900 sm:bg-army-300"
                        : " border-transparent bg-army-300 sm:bg-transparent "
                      }`}
                href={path.url}
                aria-current={pathname === path.url ? "true" : "false"}
                role="menuitem"
              >
                {path.name}
              </a>
            </li>
          ))
        }
      </ul>
      <!-- <ul class="grid grid-cols-12 sm:grid-cols-none sm:grid-flow-col-dense sm:auto-cols-max" role="menubar">
        {
          paths.map((path) => (
            <li class="contents" role="none">
              <a
                class={`col-span-4 sm:col-auto text-center ring-2 ring-inset sm:border-b-8 sm:pb-2 sm:ring-0 sm:px-3 py-3 transition-colors hover:bg-army-300 hover:underline underline sm:no-underline whitespace-nowrap  ${
                  pathname === path.url || otherPathname === path.url
                    ? "ring-army-900 border-army-900 bg-army-300"
                    : "ring-army-300 border-transparent bg-transparent"
                }`}
                href={path.url}
                aria-current={pathname === path.url ? "true" : "false"}
                role="menuitem"
              >
                {path.name}
              </a>
            </li>
          ))
        }
      </ul> -->
    </nav>
  </div>
</header>
<style is:global>
  :root {
    --nav-background: theme(colors.army.200);
  }
</style>
